@model Exception

@{
    ViewData["Title"] = "Hata";
}

<h1 class="text-danger">Bir Hata Oluþtu</h1>

@if (Model != null)
{
    <div class="alert alert-danger">
        <strong>Hata Mesajý:</strong> @Model.Message
    </div>
    <details style="white-space:pre-wrap;">
        <summary>StackTrace (detaylar)</summary>
        @Model.StackTrace
    </details>
}
else
{
    <p>Beklenmeyen bir hata oluþtu. Lütfen daha sonra tekrar deneyiniz.</p>
}
<div class="mt-3">
    <a asp-controller="ActivitiesWizard" asp-action="Index" class="btn btn-primary">Return to Home</a>
</div> 